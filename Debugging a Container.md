`docker logs <container id / container name>`:
- Displays the logs of a container. (Running image)

`docker exec -it <container id / container name> /bin/bash`:
- Open an interactive terminal (-it) for the running container inside bash 

1:13:01